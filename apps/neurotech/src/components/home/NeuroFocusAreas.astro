---
---

<section id="explore-neuro" class="bg-navy-950 text-slate-100 py-24 relative z-10 border-t border-navy-800">
  <div class="mx-auto max-w-6xl px-6">
    <div class="mb-16">
      <h2 class="text-4xl sm:text-5xl font-extrabold tracking-tight mb-4 bg-gradient-to-r from-gold-100 to-gold-500 bg-clip-text text-transparent">Our Focus Areas</h2>
      <p class="text-lg text-slate-400 max-w-2xl font-light">
        We bridge the gap between the human mind and technology through neuroscience foundations, brain-computer interfaces, advanced signal analysis, and human-first ethics.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      
      <!-- CARD 1: Understand the Brain (Span 2) -->
      <article class="bento-card md:col-span-2 lg:col-span-2 flex flex-col md:flex-row gap-8 group ml-card-wrapper" style="--card-brand-color: var(--color-gold-500);">
        <!-- Cybernetic Corner Brackets -->
        <div class="corner-tick top-left"></div>
        <div class="corner-tick top-right"></div>
        <div class="corner-tick bottom-left"></div>
        <div class="corner-tick bottom-right"></div>
        
        <div class="bento-glow"></div>
        
        <div class="bento-content md:w-1/2 flex flex-col justify-center min-h-[220px]">
          <h3 class="text-2xl sm:text-3xl font-bold mb-4 tracking-tight text-white group-hover:text-gold-300 transition-colors">Understand the Brain</h3>
          <p class="text-slate-400 text-base leading-relaxed mb-6 font-light">
            We map voluntary thoughts and cognitive pathways. Learn how the cortex functions and how brain waves form the basis of all mental-digital connections.
          </p>
          
          <!-- Lobe Info Panel -->
          <div id="bento-brain-info" class="bg-navy-950/60 border border-navy-800 rounded-xl p-4 min-h-[120px] flex flex-col justify-center transition-all duration-300">
            <div id="bento-brain-default">
              <div class="inline-flex items-center gap-1.5 text-gold-500 font-semibold text-xs tracking-wider uppercase mb-1">
                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="12" cy="12" r="10"></circle><path d="M12 16v-4"></path><path d="M12 8h.01"></path></svg>
                Interactive Lobe Map
              </div>
              <p class="text-xs text-slate-400 leading-relaxed font-light">
                Hover or tap the brain regions on the diagram to explore their functions and neurotech applications.
              </p>
            </div>
            <div id="bento-brain-active" class="hidden">
              <h4 id="bento-lobe-title" class="text-sm font-bold text-white mb-1 flex items-center gap-2">
                <span id="bento-lobe-indicator" class="w-2.5 h-2.5 rounded-full"></span>
                <span>Lobe Name</span>
              </h4>
              <p id="bento-lobe-desc" class="text-xs text-slate-400 leading-relaxed mb-2 font-light"></p>
              <p id="bento-lobe-app" class="text-[11px] text-cyan-400 font-medium border-t border-navy-800 pt-2"></p>
            </div>
          </div>
        </div>
        
        <div class="bento-visual md:w-1/2 min-h-[260px] md:min-h-full rounded-2xl relative overflow-hidden flex items-center justify-center bg-navy-950/30">
          <div class="generative-grid"></div>
          <div class="scan-sweep"></div>

          <div class="visual-brain w-full h-full relative flex items-center justify-center p-4">
            <svg class="brain-svg w-full h-full max-w-[280px] max-h-[240px]" viewBox="0 0 400 300" fill="none" xmlns="http://www.w3.org/2000/svg">
              <defs>
                <linearGradient id="b-frontal" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#ebd382" /><stop offset="100%" stop-color="#d4af37" /></linearGradient>
                <linearGradient id="b-parietal" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#f3e5ab" /><stop offset="100%" stop-color="#b8860b" /></linearGradient>
                <linearGradient id="b-occipital" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#ebd382" /><stop offset="100%" stop-color="#c9a84c" /></linearGradient>
                <linearGradient id="b-temporal" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#f7eed0" /><stop offset="100%" stop-color="#d4af37" /></linearGradient>
                <linearGradient id="b-cerebellum" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#ebd382" /><stop offset="100%" stop-color="#b8860b" /></linearGradient>
                <linearGradient id="b-stem" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#14285c" /><stop offset="100%" stop-color="#0d1b3e" /></linearGradient>
                
                <filter id="bento-glow" x="-20%" y="-20%" width="140%" height="140%">
                  <feGaussianBlur stdDeviation="8" result="blur" />
                  <feComposite in="SourceGraphic" in2="blur" operator="over" />
                </filter>
              </defs>

              <!-- Connection Lines/Paths -->
              <g class="neural-pathways opacity-25" stroke="#475569" stroke-width="1.2" fill="none" stroke-dasharray="3 4">
                <path d="M 120 180 Q 180 150 250 160" />
                <path d="M 100 120 Q 150 80 220 90" />
                <path d="M 220 90 Q 280 120 320 180" />
                <path d="M 180 200 Q 220 230 260 250" />
              </g>

              <!-- Interactive Brain Lobes -->
              <g class="brain-lobes-group">
                <!-- Brain Stem -->
                <path class="bento-lobe-path transition-all duration-300 cursor-pointer stroke-navy-950 stroke-[1.5] hover:stroke-gold-400"
                  d="M 230 220 Q 235 260 220 290 L 250 290 Q 260 260 255 220 Z"
                  fill="url(#b-stem)"
                  data-title="Brainstem"
                  data-color="#14285c"
                  data-desc="Monitors autonomic tasks such as pulse rate and respiration cycles."
                  data-app="Targeted for understanding foundational neural states and reflex actions."
                />

                <!-- Cerebellum -->
                <path class="bento-lobe-path transition-all duration-300 cursor-pointer stroke-navy-950 stroke-[1.5] hover:stroke-gold-400"
                  d="M 250 190 Q 320 180 330 230 Q 330 260 280 260 Q 240 260 245 220 Z"
                  fill="url(#b-cerebellum)"
                  data-title="Cerebellum"
                  data-color="#ebd382"
                  data-desc="Refines physical coordination, balance, and precision motor timing."
                  data-app="Crucial for refining motor prosthetic smooth movements in BCI decoding."
                />

                <!-- Occipital Lobe -->
                <path class="bento-lobe-path transition-all duration-300 cursor-pointer stroke-navy-950 stroke-[1.5] hover:stroke-gold-400"
                  d="M 270 90 Q 340 100 350 160 Q 355 190 320 190 Q 280 180 270 140 Z"
                  fill="url(#b-occipital)"
                  data-title="Occipital Lobe"
                  data-color="#ebd382"
                  data-desc="The visual brain. Interprets shape, colour, motion, and visual recognition."
                  data-app="Targeted by visual cortical prosthetics aiming to restore basic sight."
                />

                <!-- Temporal Lobe -->
                <path class="bento-lobe-path transition-all duration-300 cursor-pointer stroke-navy-950 stroke-[1.5] hover:stroke-gold-400"
                  d="M 160 160 Q 180 140 230 140 Q 280 150 270 190 Q 240 220 190 200 Q 150 180 160 160 Z"
                  fill="url(#b-temporal)"
                  data-title="Temporal Lobe"
                  data-color="#f7eed0"
                  data-desc="Responsible for hearing, memory formations, and speech recognition."
                  data-app="Explored by memory prosthetic research and auditory neural interfaces."
                />

                <!-- Parietal Lobe -->
                <path class="bento-lobe-path transition-all duration-300 cursor-pointer stroke-navy-950 stroke-[1.5] hover:stroke-gold-400"
                  d="M 180 40 Q 260 30 300 90 Q 260 130 220 140 Q 170 100 180 40 Z"
                  fill="url(#b-parietal)"
                  data-title="Parietal Lobe"
                  data-color="#f3e5ab"
                  data-desc="Governs tactile senses (touch, pressure, temperature) and spatial navigation."
                  data-app="Essential for providing sensory feedback, allowing users to 'feel' BCI devices."
                />

                <!-- Frontal Lobe -->
                <path class="bento-lobe-path transition-all duration-300 cursor-pointer stroke-navy-950 stroke-[1.5] hover:stroke-gold-400"
                  d="M 180 40 Q 140 50 100 100 Q 80 150 120 180 Q 150 160 160 160 Q 180 140 220 140 Q 170 100 180 40 Z"
                  fill="url(#b-frontal)"
                  data-title="Frontal Lobe"
                  data-color="#ebd382"
                  data-desc="The seat of decision making, reasoning, and voluntary motion. Contains the primary Motor Cortex."
                  data-app="The primary driver for motor BCIs, enabling users to move cursors by thinking."
                />
              </g>

              <!-- Gyri Detailing Lines -->
              <g class="pointer-events-none opacity-20" stroke="#000" stroke-width="1.8" fill="none" stroke-linecap="round">
                <path d="M 120 80 Q 130 90 140 70" />
                <path d="M 110 120 Q 130 110 150 130" />
                <path d="M 210 60 Q 220 80 240 70" />
                <path d="M 180 170 Q 200 180 220 160" />
                <path d="M 290 130 Q 310 140 330 120" />
              </g>

              <!-- Synapse Pulsing Dots -->
              <g class="synapse-group pointer-events-none"></g>
            </svg>
          </div>
        </div>
      </article>

      <!-- CARD 2: Translate Thought to Action (Span 1) -->
      <article class="bento-card col-span-1 flex flex-col gap-6 group" style="--card-brand-color: var(--color-gold-500);">
        <div class="corner-tick top-left"></div>
        <div class="corner-tick top-right"></div>
        <div class="corner-tick bottom-left"></div>
        <div class="corner-tick bottom-right"></div>

        <div class="bento-glow"></div>
        
        <div class="bento-visual h-48 sm:h-56 w-full rounded-2xl relative overflow-hidden flex items-center justify-center bg-navy-950/30">
          <div class="generative-grid"></div>
          <div class="scan-sweep"></div>
          
          <div class="bci-simulation w-full h-full relative flex items-center justify-center p-4">
            <svg class="bci-svg w-full h-full max-w-[200px]" viewBox="0 0 200 140" fill="none">
              <!-- Mind Node -->
              <circle cx="35" cy="70" r="16" class="mind-node animate-pulse" fill="#0a142c" stroke="#14285c" stroke-width="1.5" />
              <path d="M 30,66 A 6,6 0 0,1 40,66" stroke="#d4af37" stroke-width="2" stroke-linecap="round" />
              <circle cx="35" cy="76" r="3" fill="#d4af37" />
              
              <!-- Device Node -->
              <circle cx="165" cy="70" r="16" class="device-node" fill="#0a142c" stroke="#14285c" stroke-width="1.5" />
              <!-- Device Icon: Smart Bulb -->
              <path id="bulb-icon" d="M165,60 C159.5,60 157,64 157,68 C157,72.5 161,75 161,78 L169,78 C169,75 173,72.5 173,68 C173,64 170.5,60 165,60 Z" fill="none" stroke="#94a3b8" stroke-width="1.5" stroke-linejoin="round" />
              <line x1="162" y1="81" x2="168" y2="81" stroke="#94a3b8" stroke-width="1.5" />
              <line x1="163.5" y1="84" x2="166.5" y2="84" stroke="#94a3b8" stroke-width="1.5" />

              <!-- Connected signal wave path -->
              <path id="bento-bci-wave" d="M 51,70 Q 79,70 108,70 T 149,70" fill="none" stroke="#475569" stroke-width="2" stroke-linecap="round" />
            </svg>
            
            <!-- Concentrate Status bubble overlay -->
            <div id="bento-bci-bubble" class="bci-status-bubble font-mono">
              <span class="bubble-dot bg-[#475569] animate-ping"></span>
              <span class="bubble-text">Resting...</span>
            </div>
          </div>
        </div>
        
        <div class="bento-content mt-auto flex-1 flex flex-col justify-between">
          <div>
            <h3 class="text-xl sm:text-2xl font-bold mb-3 tracking-tight text-white group-hover:text-gold-300 transition-colors">Thought to Action</h3>
            <p class="text-slate-400 text-sm sm:text-base leading-relaxed mb-4 font-light">
              Brain-Computer Interfaces capture motor intentions and translate them into commands that trigger smart external devices.
            </p>
          </div>
          <button id="bento-bci-btn" class="w-full py-2.5 bg-navy-800 hover:bg-gold-500 text-white hover:text-navy-950 font-bold rounded-xl transition-all border border-navy-700 hover:border-gold-500 select-none hover:shadow-[0_4px_16px_rgba(212,175,55,0.25)] active:scale-[0.98] text-xs sm:text-sm tracking-wide uppercase cursor-pointer">
            Concentrate (Hold)
          </button>
        </div>
      </article>

      <!-- CARD 3: Recognise Patterns (Span 1) -->
      <article class="bento-card col-span-1 flex flex-col gap-6 group" style="--card-brand-color: var(--color-gold-500);">
        <div class="corner-tick top-left"></div>
        <div class="corner-tick top-right"></div>
        <div class="corner-tick bottom-left"></div>
        <div class="corner-tick bottom-right"></div>

        <div class="bento-glow"></div>
        
        <div class="bento-visual h-48 sm:h-56 w-full rounded-2xl relative overflow-hidden flex items-center justify-center bg-navy-950/30">
          <div class="generative-grid"></div>
          <div class="scan-sweep"></div>
          
          <div class="pattern-simulation w-full h-full relative flex items-center justify-center p-4">
            <svg class="pattern-svg w-full h-full max-w-[240px]" viewBox="0 0 200 140" fill="none">
              <!-- Grid overlay inside bento visual -->
              <g stroke="rgba(212,175,55,0.08)" stroke-width="1">
                <line x1="20" y1="35" x2="180" y2="35" />
                <line x1="20" y1="70" x2="180" y2="70" />
                <line x1="20" y1="105" x2="180" y2="105" />
                <line x1="50" y1="20" x2="50" y2="120" />
                <line x1="100" y1="20" x2="100" y2="120" />
                <line x1="150" y1="20" x2="150" y2="120" />
              </g>

              <!-- Chaotic Noisy EEG signal path (Gray/Navy, fades on hover) -->
              <path class="raw-noise-path stroke-slate-500 opacity-60 transition-all duration-500 group-hover:opacity-10 group-hover:stroke-slate-650" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"
                d="M 20,70 L 30,55 L 35,85 L 42,40 L 48,95 L 55,60 L 62,80 L 68,45 L 75,90 L 82,55 L 88,75 L 95,50 L 102,95 L 108,60 L 115,80 L 122,45 L 128,90 L 135,55 L 142,85 L 148,40 L 155,95 L 162,60 L 168,80 L 180,70" />

              <!-- Clean, Decoded Golden Wave path (Alpha wave, sweeps and glows on hover) -->
              <path class="decoded-wave-path stroke-gold-500 opacity-20 group-hover:opacity-100 group-hover:stroke-[2.5] transition-all duration-500 ease-out" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                d="M 20,70 Q 40,30 60,70 T 100,70 T 140,70 T 180,70" />

              <!-- Active pulse nodes (Highlighting target channels decoded by ML, glowing cyan/blue) -->
              <g class="decoded-nodes opacity-0 group-hover:opacity-100 transition-opacity duration-500">
                <circle cx="60" cy="70" r="4.5" fill="#06b6d4" class="animate-ping" style="transform-origin: 60px 70px;" />
                <circle cx="60" cy="70" r="3" fill="#06b6d4" stroke="#ffffff" stroke-width="1" />
                
                <circle cx="100" cy="70" r="4.5" fill="#3b82f6" class="animate-ping" style="transform-origin: 100px 70px; animation-delay: 0.3s;" />
                <circle cx="100" cy="70" r="3" fill="#3b82f6" stroke="#ffffff" stroke-width="1" />
                
                <circle cx="140" cy="70" r="4.5" fill="#06b6d4" class="animate-ping" style="transform-origin: 140px 70px; animation-delay: 0.6s;" />
                <circle cx="140" cy="70" r="3" fill="#06b6d4" stroke="#ffffff" stroke-width="1" />
              </g>

              <!-- Decoded label -->
              <text x="25" y="25" class="fill-slate-500 font-mono text-[8px] uppercase tracking-wider opacity-60">Input: EEG_RAW</text>
              <text x="135" y="25" class="fill-gold-500 font-mono text-[8px] uppercase tracking-wider opacity-0 group-hover:opacity-100 transition-opacity duration-500">Filter: 12Hz</text>
            </svg>

            <div class="pattern-badge font-mono opacity-0 group-hover:opacity-100 transition-all duration-300 border border-gold-500/25 bg-gold-500/5 text-gold-500">
              Noise Decoded: 98.4%
            </div>
          </div>
        </div>
        
        <div class="bento-content mt-auto">
          <h3 class="text-xl sm:text-2xl font-bold mb-3 tracking-tight text-white group-hover:text-gold-300 transition-colors">Recognise Patterns</h3>
          <p class="text-slate-400 text-sm sm:text-base leading-relaxed mb-2 font-light">
            Apply intelligent machine learning algorithms to isolate meaningful thoughts and patterns out of chaotic brainwaves and raw noise.
          </p>
        </div>
      </article>

      <!-- CARD 4: Innovate Responsibly (Span 1) -->
      <article class="bento-card col-span-1 flex flex-col gap-6 group" style="--card-brand-color: var(--color-gold-500);">
        <div class="corner-tick top-left"></div>
        <div class="corner-tick top-right"></div>
        <div class="corner-tick bottom-left"></div>
        <div class="corner-tick bottom-right"></div>

        <div class="bento-glow"></div>
        
        <div class="bento-visual h-48 sm:h-56 w-full rounded-2xl relative overflow-hidden flex items-center justify-center bg-navy-950/30">
          <div class="generative-grid"></div>
          <div class="scan-sweep"></div>
          
          <div class="privacy-simulation w-full h-full relative flex items-center justify-center p-4">
            <svg class="privacy-svg w-full h-full max-w-[200px]" viewBox="0 0 200 140" fill="none">
              <!-- Grid line / Pipeline path -->
              <line x1="20" y1="70" x2="180" y2="70" stroke="#14285c" stroke-width="1.5" stroke-linecap="round" />
              
              <!-- Privacy Shield -->
              <g class="privacy-shield">
                <!-- Shield Base -->
                <path d="M 90,40 C 90,40 100,36 100,32 C 100,36 110,40 110,40 C 110,75 100,95 100,98 C 100,95 90,75 90,40 Z" fill="#0a142c" stroke="#14285c" stroke-width="2" class="shield-body transition-all duration-300" />
                <path d="M 96,65 L 100,69 L 106,62" stroke="#94a3b8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="shield-check transition-all duration-300" />
              </g>

              <!-- Flowing particles representing raw requests/signals -->
              <circle cx="25" cy="70" r="4.5" fill="#14285c" class="flow-dot flow-dot-1" />
              <circle cx="55" cy="70" r="4.5" fill="#14285c" class="flow-dot flow-dot-2" />
              
              <!-- Soft protecting forcefield circle -->
              <circle cx="100" cy="55" r="42" class="forcefield-ring" stroke="#06b6d4" stroke-width="1.5" stroke-dasharray="4 6" />
            </svg>

            <div class="privacy-badge font-mono border border-gold-500/25 bg-gold-500/5 text-gold-500">Cognitive Privacy</div>
          </div>
        </div>
        
        <div class="bento-content mt-auto">
          <h3 class="text-xl sm:text-2xl font-bold mb-3 tracking-tight text-white group-hover:text-gold-300 transition-colors">Innovate Responsibly</h3>
          <p class="text-slate-400 text-sm sm:text-base leading-relaxed mb-2 font-light">
            Establish solid ethical foundations. We discuss neuro-privacy, mental integrity, and standard regulations to build safe futures.
          </p>
        </div>
      </article>

      <!-- CARD 5: Connect & Build Together (Span 1) -->
      <article class="bento-card col-span-1 flex flex-col gap-6 group md:col-span-2 lg:col-span-1" style="--card-brand-color: var(--color-gold-500);">
        <div class="corner-tick top-left"></div>
        <div class="corner-tick top-right"></div>
        <div class="corner-tick bottom-left"></div>
        <div class="corner-tick bottom-right"></div>

        <div class="bento-glow"></div>
        
        <div class="bento-visual h-48 sm:h-56 w-full rounded-2xl relative overflow-hidden flex items-center justify-center bg-navy-950/30">
          <div class="generative-grid"></div>
          <div class="scan-sweep"></div>
          
          <div class="colab-simulation w-full h-full relative flex items-center justify-center p-4">
            <svg class="colab-svg w-full h-full max-w-[200px]" viewBox="0 0 200 140" fill="none">
              <!-- Network Connections (Dendrites) -->
              <path d="M 40,40 L 100,70 M 40,100 L 100,70 M 160,40 L 100,70 M 160,100 L 100,70" stroke="#14285c" stroke-width="2" class="synapse-link" />
              
              <!-- Core Synapse Node -->
              <circle cx="100" cy="70" r="10" fill="#0a142c" stroke="#14285c" stroke-width="1.5" class="center-synapse transition-all duration-300" />
              
              <!-- Avatar Nodes (Members) -->
              <g class="member-node node-m1" transform="translate(40, 40)">
                <circle cx="0" cy="0" r="10" fill="#0a142c" stroke="#14285c" stroke-width="1.2" />
                <path d="M -6,5 A 6,6 0 0,1 6,5" stroke="#ebd382" stroke-width="1.2" />
                <circle cx="0" cy="-2" r="3.5" fill="#ebd382" />
              </g>

              <g class="member-node node-m2" transform="translate(40, 100)">
                <circle cx="0" cy="0" r="10" fill="#0a142c" stroke="#14285c" stroke-width="1.2" />
                <path d="M -6,5 A 6,6 0 0,1 6,5" stroke="#ebd382" stroke-width="1.2" />
                <circle cx="0" cy="-2" r="3.5" fill="#ebd382" />
              </g>

              <g class="member-node node-m3" transform="translate(160, 40)">
                <circle cx="0" cy="0" r="10" fill="#0a142c" stroke="#14285c" stroke-width="1.2" />
                <path d="M -6,5 A 6,6 0 0,1 6,5" stroke="#ebd382" stroke-width="1.2" />
                <circle cx="0" cy="-2" r="3.5" fill="#ebd382" />
              </g>

              <g class="member-node node-m4" transform="translate(160, 100)">
                <circle cx="0" cy="0" r="10" fill="#0a142c" stroke="#14285c" stroke-width="1.2" />
                <path d="M -6,5 A 6,6 0 0,1 6,5" stroke="#ebd382" stroke-width="1.2" />
                <circle cx="0" cy="-2" r="3.5" fill="#ebd382" />
              </g>
              
              <!-- Transmitters flowing in -->
              <circle cx="40" cy="40" r="3.5" fill="#06b6d4" class="neuro-transmitter pulse-nt-1" />
              <circle cx="40" cy="100" r="3.5" fill="#3b82f6" class="neuro-transmitter pulse-nt-2" />
              <circle cx="160" cy="40" r="3.5" fill="#06b6d4" class="neuro-transmitter pulse-nt-3" />
              <circle cx="160" cy="100" r="3.5" fill="#3b82f6" class="neuro-transmitter pulse-nt-4" />
            </svg>

            <div class="colab-badge font-mono border border-gold-500/25 bg-gold-500/5 text-gold-500">
              Synapses pulsing
            </div>
          </div>
        </div>
        
        <div class="bento-content mt-auto">
          <h3 class="text-xl sm:text-2xl font-bold mb-3 tracking-tight text-white group-hover:text-gold-300 transition-colors">Build Together</h3>
          <p class="text-slate-400 text-sm sm:text-base leading-relaxed mb-2 font-light">
            Bring ideas to life in active hardware build sessions, paper reviews, hackathons, and cross-degree prototype sessions.
          </p>
        </div>
      </article>

    </div>
  </div>
</section>

<style>
  /* Color Mix Helpers */
  :root {
    --color-gold-500: #d4af37;
    --bento-visual-bg: #070e20;
    --bento-visual-border: #14285c;
    --bento-visual-shadow: inset 0 2px 8px rgba(0, 0, 0, 0.4);
    --bento-grid-color: rgba(212, 175, 55, 0.03);
    --bento-wire-color: rgba(212, 175, 55, 0.1);
    --bento-wire-hover: rgba(212, 175, 55, 0.25);
    --bento-node-bg: #0a142c;
    --bento-node-border: rgba(212, 175, 55, 0.15);
    --bento-text-light: #94a3b8;
    --bento-canvas-card: #0a142c;
  }

  .bento-card {
    position: relative;
    background: #0a142c;
    border: 1px solid #14285c;
    border-radius: 2rem;
    padding: 2.25rem;
    overflow: hidden;
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), 
                border-color 0.4s cubic-bezier(0.16, 1, 0.3, 1),
                box-shadow 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    backdrop-filter: blur(12px);
    box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.05), 
                0 4px 24px rgba(0, 0, 0, 0.3);
  }

  /* Deep Interactive Hover Lift & Glow */
  .bento-card:hover {
    transform: translateY(-5px);
    border-color: color-mix(in srgb, var(--card-brand-color) 45%, #14285c);
    box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.1),
                0 16px 40px -10px rgba(3, 7, 18, 0.5),
                0 0 28px color-mix(in srgb, var(--card-brand-color) 12%, transparent);
  }

  /* Corner Brackets */
  .corner-tick {
    position: absolute;
    width: 8px;
    height: 8px;
    border-color: #1e3a8a;
    opacity: 0.3;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    pointer-events: none;
  }
  
  .top-left { top: 12px; left: 12px; border-top: 1.5px solid; border-left: 1.5px solid; }
  .top-right { top: 12px; right: 12px; border-top: 1.5px solid; border-right: 1.5px solid; }
  .bottom-left { bottom: 12px; left: 12px; border-bottom: 1.5px solid; border-left: 1.5px solid; }
  .bottom-right { bottom: 12px; right: 12px; border-bottom: 1.5px solid; border-right: 1.5px solid; }

  .bento-card:hover .corner-tick {
    opacity: 0.9;
    border-color: var(--card-brand-color);
    transform: scale(1.1);
  }

  .bento-glow {
    position: absolute;
    inset: 0;
    opacity: 0;
    filter: blur(50px);
    transform: scale(1.3);
    transition: opacity 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    z-index: 0;
    pointer-events: none;
    background: radial-gradient(circle at center, color-mix(in srgb, var(--card-brand-color) 12%, transparent) 0%, transparent 70%);
  }

  .bento-card:hover .bento-glow {
    opacity: 1;
  }

  .bento-content, .bento-visual {
    position: relative;
    z-index: 1;
  }

  /* Visualizers */
  .bento-visual {
    background: var(--bento-visual-bg);
    border: 1px solid var(--bento-visual-border);
    box-shadow: var(--bento-visual-shadow);
    transition: border-color 0.4s, background-color 0.4s;
  }

  .bento-card:hover .bento-visual {
    border-color: color-mix(in srgb, var(--card-brand-color) 30%, var(--bento-visual-border));
    background: #081026;
  }

  /* Technical Grid Background */
  .generative-grid {
    position: absolute;
    inset: 0;
    background-size: 16px 16px;
    background-image: 
      linear-gradient(to right, var(--bento-grid-color) 1px, transparent 1px),
      linear-gradient(to bottom, var(--bento-grid-color) 1px, transparent 1px);
    opacity: 0.8;
  }

  /* Sweeping scanline */
  .scan-sweep {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, 
      transparent 0%, 
      rgba(212, 175, 55, 0.0) 30%, 
      rgba(212, 175, 55, 0.06) 50%, 
      rgba(6, 182, 212, 0.06) 52%,
      transparent 70%
    );
    transform: translateX(-100%);
    width: 200%;
    pointer-events: none;
    z-index: 3;
  }

  .bento-card:hover .scan-sweep {
    animation: bento-sweep 2.5s cubic-bezier(0.25, 0.46, 0.45, 0.94) infinite;
  }

  @keyframes bento-sweep {
    to { transform: translateX(50%); }
  }

  /* ──────────────────────────────────────────────────────────
     CARD 1: BRAIN EXPLORER
  ────────────────────────────────────────────────────────── */
  .bento-lobe-path {
    filter: drop-shadow(0px 3px 4px rgba(0, 0, 0, 0.4));
  }

  .brain-lobes-group:hover .bento-lobe-path:not(:hover) {
    opacity: 0.25;
    filter: grayscale(40%) drop-shadow(0px 0px 0px transparent);
  }

  .bento-lobe-path:hover {
    filter: drop-shadow(0 0 8px var(--color-gold-500));
    transform: translateY(-1px);
  }

  /* Synapse Pulse animations */
  .bento-synapse {
    animation: syn-travel 4s linear infinite;
  }

  @keyframes syn-travel {
    to { stroke-dashoffset: -40; }
  }

  /* ──────────────────────────────────────────────────────────
     CARD 2: THOUGHT TO ACTION (BCI)
  ────────────────────────────────────────────────────────── */
  .bci-status-bubble {
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    color: var(--bento-text-light);
    background: var(--bento-node-bg);
    border: 1px solid var(--bento-node-border);
    padding: 0.35rem 0.75rem;
    border-radius: 9999px;
    position: absolute;
    bottom: 0.75rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    display: flex;
    align-items: center;
    gap: 0.35rem;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
    transition: all 0.3s ease;
    white-space: nowrap;
  }

  .bento-card:hover .bci-status-bubble {
    color: var(--color-gold-500);
    border-color: color-mix(in srgb, var(--color-gold-500) 30%, transparent);
    background: color-mix(in srgb, var(--color-gold-500) 4%, #0a142c);
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.12);
  }

  .bubble-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
  }

  .mind-node {
    transition: all 0.4s ease;
    transform-origin: 35px 70px;
  }
  .bento-card:hover .mind-node {
    fill: color-mix(in srgb, var(--color-gold-500) 10%, #0a142c);
    stroke: var(--color-gold-500);
    box-shadow: 0 0 10px rgba(212, 175, 55, 0.3);
  }

  .device-node {
    transition: all 0.4s ease;
    transform-origin: 165px 70px;
  }

  /* BCI Active States */
  .bci-active .mind-node {
    fill: color-mix(in srgb, var(--color-gold-500) 25%, #0a142c) !important;
    stroke: var(--color-gold-500) !important;
    transform: scale(1.15);
    filter: drop-shadow(0 0 6px var(--color-gold-500));
  }

  .bci-active .device-node {
    fill: color-mix(in srgb, var(--color-gold-500) 20%, #0a142c) !important;
    stroke: var(--color-gold-500) !important;
    transform: scale(1.15);
    filter: drop-shadow(0 0 10px var(--color-gold-500));
  }

  .bci-active #bulb-icon {
    stroke: var(--color-gold-500) !important;
    fill: color-mix(in srgb, var(--color-gold-500) 40%, transparent) !important;
    filter: drop-shadow(0 0 6px var(--color-gold-500));
  }

  /* ──────────────────────────────────────────────────────────
     CARD 3: RECOGNISE PATTERNS
  ────────────────────────────────────────────────────────── */
  .pattern-simulation {
    overflow: visible;
  }
  
  .pattern-svg {
    overflow: visible;
  }

  .pattern-badge {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    font-size: 0.6rem;
    font-weight: 700;
    color: var(--color-gold-500);
    border: 1px solid color-mix(in srgb, var(--color-gold-500) 20%, #14285c);
    background: color-mix(in srgb, var(--color-gold-500) 5%, #0a142c);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    z-index: 2;
  }

  /* ──────────────────────────────────────────────────────────
     CARD 4: INNOVATE RESPONSIBLY
  ────────────────────────────────────────────────────────── */
  .privacy-simulation {
    overflow: visible;
  }
  
  .privacy-svg {
    overflow: visible;
  }

  .shield-body {
    transform-origin: 100px 65px;
  }

  .bento-card:hover .shield-body {
    fill: color-mix(in srgb, var(--color-gold-500) 15%, #0a142c);
    stroke: var(--color-gold-500);
    transform: scale(1.08);
    filter: drop-shadow(0 0 6px var(--color-gold-500));
  }

  .bento-card:hover .shield-check {
    stroke: var(--color-gold-500);
  }

  .forcefield-ring {
    transform-origin: 100px 55px;
    opacity: 0.15;
    transition: all 0.6s ease;
  }

  .bento-card:hover .forcefield-ring {
    opacity: 0.6;
    animation: force-pulse 2s infinite alternate;
  }

  @keyframes force-pulse {
    from { transform: scale(1); opacity: 0.4; }
    to { transform: scale(1.06); opacity: 0.7; filter: drop-shadow(0 0 4px #06b6d4); }
  }

  .flow-dot {
    animation: privacy-flow 4s linear infinite;
  }

  .flow-dot-1 { animation-delay: 0s; }
  .flow-dot-2 { animation-delay: 2s; }

  @keyframes privacy-flow {
    0% { cx: 20px; opacity: 0; fill: #14285c; }
    10% { opacity: 0.8; }
    /* blocked at shield border around x=90 */
    42% { cx: 88px; opacity: 0.8; fill: #14285c; transform: scale(1); }
    46% { cx: 90px; opacity: 0; fill: var(--color-gold-500); transform: scale(0.6); }
    100% { cx: 90px; opacity: 0; }
  }

  .privacy-badge {
    position: absolute;
    bottom: 0.75rem;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.65rem;
    font-weight: 700;
    color: var(--bento-text-light);
    border: 1px solid var(--bento-node-border);
    background: var(--bento-node-bg);
    padding: 0.35rem 0.75rem;
    border-radius: 9999px;
    white-space: nowrap;
    opacity: 0.6;
    transition: all 0.3s ease;
  }

  .bento-card:hover .privacy-badge {
    opacity: 1;
    color: var(--color-gold-500);
    border-color: color-mix(in srgb, var(--color-gold-500) 35%, transparent);
    background: color-mix(in srgb, var(--color-gold-500) 4%, #0a142c);
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.12);
  }

  /* ──────────────────────────────────────────────────────────
     CARD 5: CONNECT & BUILD TOGETHER
  ────────────────────────────────────────────────────────── */
  .colab-simulation {
    overflow: visible;
  }

  .colab-svg {
    overflow: visible;
  }

  .synapse-link {
    transition: stroke 0.3s;
  }

  .bento-card:hover .synapse-link {
    stroke: color-mix(in srgb, var(--color-gold-500) 25%, #14285c);
  }

  .center-synapse {
    transform-origin: 100px 70px;
  }

  .bento-card:hover .center-synapse {
    fill: color-mix(in srgb, var(--color-gold-500) 30%, #0a142c);
    stroke: var(--color-gold-500);
    transform: scale(1.22);
    filter: drop-shadow(0 0 8px var(--color-gold-500));
  }

  /* Pulsing transmitter sparks */
  .neuro-transmitter {
    opacity: 0.85;
    animation: nt-pulse 2s cubic-bezier(0.25, 0.46, 0.45, 0.94) infinite;
  }

  .pulse-nt-1 { transform-origin: 40px 40px; }
  .pulse-nt-2 { transform-origin: 40px 100px; animation-delay: 0.5s; }
  .pulse-nt-3 { transform-origin: 160px 40px; animation-delay: 1s; }
  .pulse-nt-4 { transform-origin: 160px 100px; animation-delay: 1.5s; }

  @keyframes nt-pulse {
    0% { transform: translate(0,0) scale(0.9); opacity: 0; }
    10% { opacity: 1; }
    90% { opacity: 1; }
    100% {
      /* Travel to center (100,70) from respective starts */
      transform: translate(var(--tx, 0px), var(--ty, 0px)) scale(1.1);
      opacity: 0;
    }
  }

  /* Specific translation coordinates to center (100, 70) */
  .pulse-nt-1 { --tx: 60px; --ty: 30px; }
  .pulse-nt-2 { --tx: 60px; --ty: -30px; }
  .pulse-nt-3 { --tx: -60px; --ty: 30px; }
  .pulse-nt-4 { --tx: -60px; --ty: -30px; }

  .bento-card:hover .neuro-transmitter {
    animation-duration: 0.9s; /* Accelerate transmitters on hover! */
  }

  .colab-badge {
    position: absolute;
    bottom: 0.75rem;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.6rem;
    font-weight: 700;
    color: var(--color-gold-500);
    border: 1px solid color-mix(in srgb, var(--color-gold-500) 20%, #14285c);
    background: color-mix(in srgb, var(--color-gold-500) 5%, #0a142c);
    padding: 0.25rem 0.55rem;
    border-radius: 4px;
    z-index: 2;
  }

  /* Multiplayer nodes */
  .member-node {
    transition: transform 0.3s ease;
  }

  .bento-card:hover .member-node {
    transform: scale(1.08) translate(var(--mx, 0px), var(--my, 0px));
  }

  .node-m1 { --mx: -2px; --my: -2px; transform-origin: 40px 40px; }
  .node-m2 { --mx: -2px; --my: 2px; transform-origin: 40px 100px; }
  .node-m3 { --mx: 2px; --my: -2px; transform-origin: 160px 40px; }
  .node-m4 { --mx: 2px; --my: 2px; transform-origin: 160px 100px; }

  /* ──────────────────────────────────────────────────────────
     RESPONSIVE AND ACCESSIBILITY
  ────────────────────────────────────────────────────────── */
  @media (max-width: 768px) {
    .bento-card {
      padding: 1.75rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .bento-card { transition: none; }
    .bento-card:hover { transform: none; box-shadow: none; }
    .corner-tick { transition: none; }
    .scan-sweep { display: none; }
    .bento-synapse { animation: none; }
    .flow-dot { animation: none; opacity: 0.5; cx: 60px; }
    .forcefield-ring { animation: none; opacity: 0.2; }
    .neuro-transmitter { animation: none; transform: translate(30px, 15px); opacity: 0.5; }
    .colab-cursor { display: none; }
    .raw-noise-path { transition: none; }
    .decoded-wave-path { transition: none; }
  }
</style>

<script>
  // --- CARD 1: Brain Lobe Information panel mapping ---
  const lobes = document.querySelectorAll('.bento-lobe-path');
  const defaultPanel = document.getElementById('bento-brain-default');
  const activePanel = document.getElementById('bento-brain-active');
  const lobeTitle = document.getElementById('bento-lobe-title');
  const lobeIndicator = document.getElementById('bento-lobe-indicator');
  const lobeDesc = document.getElementById('bento-lobe-desc');
  const lobeApp = document.getElementById('bento-lobe-app');

  let selectedLobe: SVGElement | null = null;

  const showLobeInfo = (target: SVGElement) => {
    const title = target.getAttribute('data-title');
    const color = target.getAttribute('data-color') || '#d4af37';
    const desc = target.getAttribute('data-desc');
    const app = target.getAttribute('data-app');

    if (lobeTitle && title) lobeTitle.lastElementChild!.textContent = title;
    if (lobeIndicator && color) {
      lobeIndicator.style.backgroundColor = color;
      lobeIndicator.style.boxShadow = `0 0 8px ${color}`;
    }
    if (lobeDesc && desc) lobeDesc.textContent = desc;
    if (lobeApp && app) {
      lobeApp.innerHTML = `<span class="font-bold text-slate-200">Neurotech Link:</span> ${app}`;
    }

    if (defaultPanel && activePanel) {
      defaultPanel.classList.add('hidden');
      activePanel.classList.remove('hidden');
    }
  };

  const resetLobeInfo = () => {
    if (selectedLobe) return;
    if (defaultPanel && activePanel) {
      defaultPanel.classList.remove('hidden');
      activePanel.classList.add('hidden');
    }
  };

  lobes.forEach(lobe => {
    const svgLobe = lobe as SVGElement;
    svgLobe.setAttribute('tabindex', '0');
    svgLobe.setAttribute('role', 'button');
    svgLobe.setAttribute('aria-label', `Explore ${svgLobe.getAttribute('data-title')}`);

    lobe.addEventListener('mouseenter', (e) => {
      showLobeInfo(e.target as SVGElement);
    });

    lobe.addEventListener('focus', (e) => {
      showLobeInfo(e.target as SVGElement);
    });

    lobe.addEventListener('click', (e) => {
      const target = e.target as SVGElement;
      selectedLobe = selectedLobe === target ? null : target;
      showLobeInfo(target);
      lobes.forEach(item => item.removeAttribute('aria-pressed'));
      if (selectedLobe) selectedLobe.setAttribute('aria-pressed', 'true');
    });

    lobe.addEventListener('mouseleave', () => {
      resetLobeInfo();
    });

    lobe.addEventListener('blur', () => {
      resetLobeInfo();
    });

    lobe.addEventListener('keydown', (e) => {
      const kb = e as KeyboardEvent;
      if (kb.key === 'Enter' || kb.key === ' ') {
        kb.preventDefault();
        (e.target as SVGElement).dispatchEvent(new MouseEvent('click', { bubbles: true }));
      }
      if (kb.key === 'Escape') {
        selectedLobe = null;
        lobes.forEach(item => item.removeAttribute('aria-pressed'));
        resetLobeInfo();
      }
    });
  });

  // --- Card 1: Add dynamic floating synapses inside the brain pathways ---
  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (!prefersReducedMotion) {
    const pathways = document.querySelectorAll('.neural-pathways path');
    const synapseGroup = document.querySelector('.synapse-group');
    if (pathways.length > 0 && synapseGroup) {
      pathways.forEach((path, idx) => {
        const svgPath = path as SVGPathElement;
        const length = svgPath.getTotalLength();

        for (let i = 0; i < 2; i++) {
          const particle = document.createElementNS("http://www.w3.org/2000/svg", "circle");
          particle.setAttribute("r", "2");
          particle.setAttribute("fill", i === 0 ? "#06b6d4" : "#3b82f6");
          particle.setAttribute("opacity", "0.9");
          synapseGroup.appendChild(particle);

          let progress = (i * 0.5) + (idx * 0.2);
          const speed = 0.0015 + (Math.random() * 0.0015);

          const animateParticle = () => {
            progress += speed;
            if (progress > 1) progress = 0;
            const pt = svgPath.getPointAtLength(progress * length);
            particle.setAttribute("cx", pt.x.toString());
            particle.setAttribute("cy", pt.y.toString());
            requestAnimationFrame(animateParticle);
          };
          requestAnimationFrame(animateParticle);
        }
      });
    }
  }

  // --- CARD 2: BCI WAVE AND CONCENTRATE INTERACTION ---
  const bciBtn = document.getElementById('bento-bci-btn');
  const bciWavePath = document.getElementById('bento-bci-wave');
  const bciBubble = document.getElementById('bento-bci-bubble');
  const bciVisualContainer = document.querySelector('.bci-simulation');

  if (bciBtn && bciWavePath && bciBubble && bciVisualContainer) {
    const bubbleDot = bciBubble.querySelector('.bubble-dot') as HTMLSpanElement;
    const bubbleText = bciBubble.querySelector('.bubble-text') as HTMLSpanElement;

    let isPressing = false;
    let amplitude = 2;
    let time = 0;

    const startConcentrating = () => {
      isPressing = true;
      bciBtn.textContent = 'Concentrating...';
      bciVisualContainer.classList.add('bci-active');
    };

    const stopConcentrating = () => {
      isPressing = false;
      bciBtn.textContent = 'Concentrate (Hold)';
      bciVisualContainer.classList.remove('bci-active');
    };

    bciBtn.addEventListener('mousedown', startConcentrating);
    bciBtn.addEventListener('touchstart', startConcentrating, { passive: true });
    window.addEventListener('mouseup', stopConcentrating);
    window.addEventListener('touchend', stopConcentrating, { passive: true });

    const animateBCI = () => {
      if (prefersReducedMotion) {
        amplitude = isPressing ? 25 : 2;
      } else {
        if (isPressing) {
          amplitude += (30 - amplitude) * 0.08;
        } else {
          amplitude += (2 - amplitude) * 0.12;
        }
        time += isPressing ? 0.35 : 0.08;
      }

      let pathD = `M 51,70 `;
      for (let x = 51; x <= 149; x += 3) {
        const fade = Math.sin((x - 51) / 98 * Math.PI);
        const y = 70 + Math.sin(x * 0.16 - time) * amplitude * fade;
        pathD += `L ${x},${y} `;
      }
      bciWavePath.setAttribute('d', pathD);

      if (amplitude > 20) {
        bciWavePath.setAttribute('stroke', '#06b6d4');
        bciWavePath.setAttribute('stroke-width', '3');
        if (bubbleDot && bubbleText) {
          bubbleDot.className = 'bubble-dot bg-cyan-400 animate-ping';
          
          if (amplitude > 26) {
            bubbleText.textContent = 'Light Triggered! 💡';
            bubbleDot.className = 'bubble-dot bg-cyan-500 animate-pulse';
          } else {
            bubbleText.textContent = 'Decoding motor intent...';
          }
        }
      } else {
        bciWavePath.setAttribute('stroke', '#475569');
        bciWavePath.setAttribute('stroke-width', '2');
        if (bubbleDot && bubbleText) {
          bubbleDot.className = 'bubble-dot bg-slate-500 animate-ping';
          bubbleText.textContent = isPressing ? 'Focusing...' : 'Resting...';
        }
      }

      requestAnimationFrame(animateBCI);
    };

    animateBCI();
  }
</script>
